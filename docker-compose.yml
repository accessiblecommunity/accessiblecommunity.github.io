version: "3.7"

services:
  website:
    container_name: astro
    build:
      context: ./src
      target: dev-test
    restart: always
    ports:
      - 4321:4321
      - 4322:4322
    volumes:
      - '.:/website'
